<UserControl x:Class="LibraryBooks.MVVM.View.AddAuthorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryBooks.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Author Name:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding AuthorName}"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Publishing Date:"/>
        <DatePicker Grid.Row="1" Grid.Column="1" SelectedDate="{Binding PublishingDate}"/>

        <Button Grid.Row="3" Grid.Column="1" Content="Add" Command="{Binding AddCommand}"/>
    </Grid>
</UserControl>
